apiVersion: v1
kind: ServiceAccount
metadata:
  name: patch-route-sa
  namespace: webportal
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: patch-route-role
  namespace: webportal
rules:
  - apiGroups: ["route.openshift.io"]
    resources: ["routes"]
    verbs: ["get", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: patch-route-binding
  namespace: webportal
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: patch-route-role
subjects:
  - kind: ServiceAccount
    name: patch-route-sa
    namespace: webportal
---
apiVersion: batch/v1
kind: Job
metadata:
  name: patch-route-link
  namespace: webportal
  annotations:
    argocd.argoproj.io/hook: PostSync
spec:
  template:
    spec:
      containers:
        - name: patch
          image: quay.io/openshift/origin-cli:latest
          command:
            - /bin/sh
            - -c
            - |
              host=$(oc get route webportal -n webportal -o jsonpath='{.spec.host}')
              oc annotate route webportal -n webportal \
                "link.argocd.argoproj.io/external-link=http://$host" --overwrite
      restartPolicy: OnFailure
      serviceAccountName: patch-route-sa
